# Repository guidance

- Keep this a vendor-aware learning repository, not an operational runbook.
- Label facts, vendor terminology, and engineering inferences in
  `docs/references.md`.
- Keep Mermaid diagrams ASCII-only and use a light theme with dark text.
- Run `./scripts/validate.sh` and the Python example before handoff.
